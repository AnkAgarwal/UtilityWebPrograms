MainCaller is the main class that invokes the thread for performing the operation specified in the problem statement.
These thread class display the ID along with the action that is being performed on these ID.

A scheduler thread is created that runs in background and checks for de-duplication of the data.